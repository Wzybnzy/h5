<!DOCTYPE html>
<html>
<head>
	<title>试卷</title>
	<meta charset="utf-8" />
	<meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<meta name="apple-mobile-web-app-capable" content="yes">
	<meta HTTP-EQUIV="pragma" CONTENT="no-cache">
	<meta HTTP-EQUIV="Cache-Control" CONTENT="no-cache, must-revalidate">
	<meta HTTP-EQUIV="expires" CONTENT="0">
	<style type="text/css">
	h3{white-space:pre-wrap}
	</style>
</head>
<body>
<center>
<h1>P0147-移动开发9</h1>
<h3>
学院:_______________ 班级:_______________ 姓名:_______________ 得分:_______________   总分:100分  
</h3>
</center>
<div id="exampaper-body" style="padding: 0px">
	<!-- 开始加载试卷 -->
			单选题  每道题（5分）
				<h3><pre>1、以下哪个对象支持上传文件？</pre></h3>
					<p>A:new FileReader（）</p>
					<p>B:new File（）</p>
					<p>C:new UpFile（）</p>
					<p>D:new Load（）</p>
			
				<h3><pre>2、以下哪个事件用来监听页面加载成功？</pre></h3>
					<p>A:onsuccess</p>
					<p>B:onload</p>
					<p>C:oninput</p>
					<p>D:onchange</p>
			
				<h3><pre>3、var a = 20;
       setTimeout(function(){
          var a = 30;
       },0)
       console.log(a)的打印结果是（    ）？</pre></h3>
					<p>A:程序报错</p>
					<p>B:20</p>
					<p>C:undefined</p>
					<p>D:30</p>
			
				<h3><pre>4、var foo=”ba”;
       (function(){
           var bar=”wei”;
           console.log(foo+bar);
       })();
       console.log(foo+bar);以上代码最后一次打印的结果是？</pre></h3>
					<p>A:undefined</p>
					<p>B:报错：bar is not defined</p>
					<p>C:weiba</p>
					<p>D:bawei</p>
			
				<h3><pre>5、ajax设置同步或异步请求使用哪个属性？</pre></h3>
					<p>A:async</p>
					<p>B:dataType</p>
					<p>C:success</p>
					<p>D:data</p>
			
				<h3><pre>6、var a = [1,2,3],
    b = a;
b = a.push(4);
console.log(a,b);的结果为？</pre></h3>
					<p>A:？[1, 2, 3, 4] 4</p>
					<p>B:？[1, 2, 3,4] ？[1, 2, 3,4]</p>
					<p>C:？[1, 2, 3] ？[1, 2, 3,4]</p>
					<p>D:以上都不对</p>
			多选题  每道题（5分）
				<h3><pre>7、JSON只有两种数据格式，分别是？</pre></h3>
					<p>A:数值</p>
					<p>B:对象</p>
					<p>C:数组</p>
					<p>D:字符串</p>
			
				<h3><pre>8、关于call和apply的说法正确的有？</pre></h3>
					<p>A:call方法会在绑定this时自动执行函数</p>
					<p>B:call方法不会在绑定this时自动执行函数</p>
					<p>C:apply方法会在绑定this时自动执行函数</p>
					<p>D:apply方法不会在绑定this时自动执行函数</p>
			
				<h3><pre>9、在js中改变函数内部this指向的方法有哪些？</pre></h3>
					<p>A:apply</p>
					<p>B:bind</p>
					<p>C:call</p>
					<p>D:arguments</p>
			判断题  每道题（5分）
				<h3><pre>10、readAsDataURL（）用来设置文件上传对象</pre></h3>
				<p>A:正确</p>
				<p>B:错误</p>
			
				<h3><pre>11、使用oninput事件监听文本域变化</pre></h3>
				<p>A:正确</p>
				<p>B:错误</p>
			
				<h3><pre>12、文件域只能上传一个文件不能上传多个文件</pre></h3>
				<p>A:正确</p>
				<p>B:错误</p>
			填空题  每道题（5分）
				<h3><pre>13、页面中添加音频的标签是_______</pre></h3>
			
			
				<h3><pre>14、 页面中添加视频的标签是_______</pre></h3>
			
			
				<h3><pre>15、音视频通过_______属性调取文件路径</pre></h3>
			
			
				<h3><pre>16、表示自动播放的属性是_______</pre></h3>
			
			
				<h3><pre>17、重复播放的属性是_______</pre></h3>
			
			
				<h3><pre>18、将音视频设置为静音的属性是______</pre></h3>
			
			
				<h3><pre>19、在音频里对媒介资源管理的标签是_______</pre></h3>
			
			
				<h3><pre>20、设置音频在点击时控制播放的方法是_______</pre></h3>
			
</div>
</body>
</html>
